#! /usr/bin/env bash

set -euo pipefail
dir="$( cd "$( dirname "${BASH_SOURCE[0]}")" && pwd )"

SKEL="$dir/til.skel"
AWK_TITLE="$dir/add-title.awk"

log() {
  printf '%s\n' "$@" >&2
}

die() {
  log "$@"
  exit 1
}

title_sub() {
  title="$1"
  file="$2"
  awk -v title="$title" -f "$AWK_TITLE" "$SKEL" > "$file"
}

main() {
  :
}
